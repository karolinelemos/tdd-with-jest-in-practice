{
  "scripts": {
    "test": "jest --coverage --colors",
    "test:watch": "npm test -- --watchAll"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.5.5",
    "babel-jest": "^15.0.0",
    "babel-preset-env": "^1.7.0",
    "chai": "^4.2.0",
    "jest-cli": "^24.8.0"
  }
}
